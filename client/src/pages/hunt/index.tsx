import { FC, ReactElement } from "react";
import { View, Text } from "@tarojs/components";

const Hunt: FC = (): ReactElement => {
  return (
    <>
      <View>看看其他人在求购什么</View>
    </>
  );
};

export default Hunt;
